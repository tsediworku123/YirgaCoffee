# Nixpacks configuration for Railway deployment
# Installs build tools needed for better-sqlite3 native compilation

[phases.setup]
nixPkgs = ["python3", "gcc", "gnumake", "nodejs"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "node server/index.js"
